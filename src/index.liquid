---
layout: base
title: Home
---

<!-- Hero Section -->
<div class="hero-section">
    <div class="hero-content">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="hero-message">
                    <p class="hero-greeting mb-3">
                        Well, well, well... 👴
                    </p>
                    <p class="hero-description mb-4">
                        When I was your age, we didn't have all these fancy tutorials, Stack Overflow answers, or AI chatbots. 
                        We had to bang our heads against the wall until something worked. Now I'm passing along 
                        what I've learned from years of building open source software, solving problems the hard way, 
                        and tinkering with Python, Kubernetes, web development, and whatever else I managed to get myself into.
                    </p>
                    <p class="hero-invitation mb-4">
                        Feel free to browse around, check out some posts, or reach out to me if you want to talk or have questions.
                    </p>
                    <div class="hero-signature mb-4">
                        <p class="mb-0 text-center">
                            - Graham Dumpleton
                        </p>
                    </div>
                    <hr class="hero-divider mb-4">
                    <div class="hero-actions d-flex flex-wrap justify-content-center gap-3">
                        <a href="/posts/" class="btn btn-light">
                            <i class="bi bi-journal-text me-2"></i>Browse Posts
                        </a>
                        <a href="/guides/" class="btn btn-light">
                            <i class="bi bi-book me-2"></i>Browse Guides
                        </a>
                        <a href="/about/" class="btn btn-outline-light">
                            <i class="bi bi-person me-2"></i>More About Me
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Recent Posts Section -->
<div class="row">
    <div class="col-12">
        <div class="d-flex align-items-center justify-content-between mb-4">
            <h2 class="h3 fw-bold mb-0">
                <i class="bi bi-clock-history me-2"></i>Recent Posts
            </h2>
            <!--
            <a href="/posts/" class="btn btn-outline-primary">
                View All Posts <i class="bi bi-arrow-right ms-1"></i>
            </a>
            -->
        </div>
    </div>
</div>

<div class="row">
    {% for post in collections.posts limit:6 %}
    <div class="col-lg-6 mb-4">
        <article class="card h-100 border-0 shadow-sm post-list-item">
            <div class="card-body p-4">
                <div class="d-flex align-items-center mb-3">
                    <div class="bg-light text-muted rounded-circle d-flex align-items-center justify-content-center me-3" style="width: 40px; height: 40px;">
                        <i class="bi bi-calendar3"></i>
                    </div>
                    <div>
                        <time class="text-muted small">{{ post.date | date }}</time>
                    </div>
                </div>
                
                <h3 class="h5 fw-bold mb-3">
                    <a href="{{ post.url }}" class="text-decoration-none stretched-link">
                        {{ post.data.title }}
                    </a>
                </h3>
                
                {% if post.data.description %}
                <div class="text-muted mb-3">{{ post.data.description | markdown_no_links | truncate: 120 }}</div>
                {% endif %}
                
                {% if post.data.tags and post.data.tags.length > 0 %}
                <div class="d-flex flex-wrap gap-1">
                    {% for tag in post.data.tags | limit(3) %}
                    <span class="badge bg-light text-dark border">
                        <i class="bi bi-tag me-1"></i>{{ tag }}
                    </span>
                    {% endfor %}
                    {% if post.data.tags.length > 3 %}
                    <span class="badge bg-light text-muted border">
                        +{{ post.data.tags.length | minus: 3 }} more
                    </span>
                    {% endif %}
                </div>
                {% endif %}
            </div>
        </article>
    </div>
    {% endfor %}
</div>

<!-- Call to Action -->
<div class="row mt-5">
    <div class="col-12">
        <div class="card border-0 shadow-sm">
            <div class="card-body text-center p-5">
                <h3 class="h4 fw-bold mb-3">Stay Updated</h3>
                <p class="text-muted mb-4">
                    Follow my journey and get notified about new posts and projects.
                </p>
                <div class="d-flex flex-wrap justify-content-center gap-3 mb-4">
                    <a href="https://github.com/GrahamDumpleton" class="btn btn-dark" target="_blank" rel="noopener">
                        <i class="bi bi-github me-2"></i>Follow on GitHub
                    </a>
                    <a href="https://mastodon.social/@GrahamDumpleton" class="btn btn-info" target="_blank" rel="noopener">
                        <i class="bi bi-mastodon me-2"></i>Follow on Mastodon
                    </a>
                    <a href="https://twitter.com/GrahamDumpleton" class="btn btn-primary" target="_blank" rel="noopener">
                        <i class="bi bi-twitter me-2"></i>Follow on Twitter
                    </a>
                    <a href="https://linkedin.com/in/grahamdumpleton" class="btn btn-outline-secondary" target="_blank" rel="noopener">
                        <i class="bi bi-linkedin me-2"></i>Follow on LinkedIn
                    </a>
                </div>
                <div class="rss-info">
                    <p class="text-muted mb-2">
                        <i class="bi bi-rss me-2"></i>You can also follow this site via 
                        <a href="/feed.xml" class="text-decoration-none">RSS feed</a>.
                    </p>
                    <p class="text-muted mb-0 small">
                        If you're a Python developer, you might also want to check out the RSS feed available from 
                        <a href="https://planetpython.org/" class="text-decoration-none" target="_blank" rel="noopener">planetpython.org</a> 
                        for posts from various Python developers, including myself.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Sponsor Me Section -->
<div class="row mt-5">
    <div class="col-12">
        <div class="card border-0 shadow-sm">
            <div class="card-body text-center p-5">
                <div class="bg-warning text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 60px; height: 60px;">
                    <i class="bi bi-heart-fill fs-3"></i>
                </div>
                <h3 class="h4 fw-bold mb-3">Sponsor Me</h3>
                <p class="text-muted mb-4">
                    Support my open source work and help me continue creating tools that benefit the developer community.
                </p>
                <a href="https://github.com/sponsors/GrahamDumpleton" class="btn btn-warning btn-lg" target="_blank" rel="noopener">
                    <i class="bi bi-heart-fill me-2"></i>Sponsor on GitHub
                </a>
            </div>
        </div>
    </div>
</div>
